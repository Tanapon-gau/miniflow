server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # serve static assets directly; fall back to index.html for client-side routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
